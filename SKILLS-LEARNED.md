# SKILLS-LEARNED.md

## Ce am standardizat deja în repo (bază pentru Awwwards)

- Monorepo discipline: `npm ci` strict + lockfile sync pentru workspaces.
- Route-level pages în SPA (fără router) pentru landing split (Home/RWA/CET AI).
- SEO minim per rută: title/description/canonical + sitemap.
- Testare: `verify:all` include E2E (Playwright stable) ca “quality gate real”.
- PWA: offline fallback + cache policy controlată în Workbox.

## Skill-uri de construit pentru nivel SOTD

- WebGL/R3F scene architecture (scene manager, asset pipeline, perf budgets).
- Shader authoring (GLSL) + postprocessing pipeline.
- Motion choreography (GSAP timelines + ScrollTrigger fără jank).
- Performance engineering (split points, prefetch strategy, CWV).
- Storytelling/copy systems (narativ “RWA proof surface” + “AI agents mesh”).
