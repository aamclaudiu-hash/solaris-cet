# GitHub Actions helper scripts

| Script | Used by |
|--------|---------|
| [update_state.py](./update_state.py) | Workflow that refreshes `app/public/api/state.json` (DeDust / TonAPI) |

Run locally only when debugging the workflow; requires `requests` and env vars as in the YAML job.
